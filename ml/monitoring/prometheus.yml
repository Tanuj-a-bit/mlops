global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'recommendation-api'
    static_configs:
      - targets: ['host.docker.internal:8082', 'host.docker.internal:8080'] # Metrics from local ML service AND backend API
